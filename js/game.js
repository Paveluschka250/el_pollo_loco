// Diese Variablen werden jetzt in index.html verwaltet
// let canvas;
// let world;
// let keyboard = new Keyboard();

// init() wird jetzt in index.html aufgerufen
// Das Spiel startet erst wenn "Spiel Starten" gedrückt wird

// Keyboard-Events werden jetzt in der World-Klasse verwaltet
// Diese Event-Listener sind nicht mehr nötig, da das Keyboard-Objekt
// direkt in der World-Klasse erstellt wird
